@page "/counter"

<h1>Counter</h1>
<p>Current count: @currentCount</p>
<button class="btn btn-primary" @onclick="Count">Click me</button><br/>

<p>Current count: @currentCountin</p>
<button class="btn btn-primary" @onclick="incrementcount">incrementcount</button>

<p>Current count: @currentCountde</p>
<button class="btn btn-primary" @onclick="decrementcount">decrementcount</button>


@code {
    private int currentCount = 0;
    [Parameter]
    public int IncrementAmount { get; set; } = 0;
    public int  dcrementAmount { get; set; } = 1;
    private int currentCountin=0;

    private int currentCountde=1;

    private void Count()
    {
        currentCount++;
    }
    private void incrementcount()
{
    
currentCountin+= IncrementAmount;
    

        
}   
 private void decrementcount()
{    
    currentCountde -= IncrementAmount;
    
}   
}